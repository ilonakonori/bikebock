<%= render 'requests/index' %>
<h3>Conversations</h3>
<% @conversations.each do |c| %>
  <p><%= link_to c.opposed_user(current_user).name, conversation_path(c, anchor: "message-#{c.messages.last.id}") %></p>
<% end %>
