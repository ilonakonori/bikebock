<%= simple_form_for [ booking, review ] do |f| %>
  <%= f.input :content, label: "Describe your experience" %>
  <%= f.input :rating, collection: 1..5, class: 'vote-stars' %>
  <% if review.persisted? %>
    <%= f.submit "Save", class: 'brown-btn' %>
  <% else %>
    <%= f.submit "Add review", class: 'brown-btn' %>
  <% end %>
<% end%>
